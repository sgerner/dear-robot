services:
  dear-robot:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      DATA_DIR: /data
      PORT: 3000
    volumes:
      - dear-robot-data:/data

volumes:
  dear-robot-data:
